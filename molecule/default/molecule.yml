---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: hera-dev
    hostname: hera-dev
    image: "kilip/molecule-${MOLECULE_DIST:-ubuntu}:${MOLECULE_DIST_VERSION:-20.04}"
    pre_build_image: true
    privileged: true
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    groups: ["master", "proxy", "mysql", "etcd", "k3s_cluster"]
  - name: aion-dev
    hostname: aion-dev
    image: "kilip/molecule-${MOLECULE_DIST:-ubuntu}:${MOLECULE_DIST_VERSION:-20.04}"
    pre_build_image: true
    privileged: true
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    groups: ["master", "etcd", "k3s_cluster"]
  - name: mars-dev
    hostname: mars-dev
    image: "kilip/molecule-${MOLECULE_DIST:-ubuntu}:${MOLECULE_DIST_VERSION:-20.04}"
    pre_build_image: true
    privileged: true
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    groups: ["master","etcd", "k3s_cluster"]

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "../../roles"
    MOLECULE_NO_LOG: false
    K3S_DOWNLOAD_URL: "${K3S_DOWNLOAD_URL:-https://github.com/rancher/k3s/releases/download}"
  inventory:
    links:
      #hosts: "./inventory/hosts.ini"
      group_vars: "./inventory/group_vars"
      host_vars: "./inventory/host_vars"
  playbooks:
    converge: "../../playbook-converge.yml"
    #converge: "../../.scratches/debug.yml"
    verify: "../../playbook-verify.yml"
verifier:
  name: ansible