---
dist: focal
addons:
  snaps:
    - name: task
      confinement: classic

language: python
services: docker

env:
  matrix:
    - MOLECULE_DIST: ubuntu:20.04
      SCENARIO: default

install:
  # Install test dependencies.
  - touch .env
  - task deps

script:
  # Run tests.
  - molecule test -s "${SCENARIO}"